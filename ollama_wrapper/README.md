# `ollama` wrapper

For local sentiment labeling by LLMs, `ollama` is used to host an LLM. This wrapper uses HTTP endpoints exposed locally by `ollama` to send parameterized queries and fetch responses. 